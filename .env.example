# Supabase Configuration (Optional)
# If these are not provided, the app will run in local mode
# NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Local Mode
# When Supabase variables are not set, the app will automatically
# use localStorage for data persistence and local authentication